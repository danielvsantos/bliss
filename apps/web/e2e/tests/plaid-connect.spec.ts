import { test } from '@playwright/test';

/**
 * Plaid bank connection E2E tests
 *
 * These tests cover the Plaid Link flow and account connection management.
 * Currently scaffolded as todos — implement once Plaid sandbox credentials
 * are available in the E2E environment.
 */
test.describe('Plaid Bank Connection', () => {
  test.skip('user can connect a bank account via Plaid Link', async () => {});
  test.skip('connected account appears in the Accounts page', async () => {});
  test.skip('user can disconnect a bank account', async () => {});
  test.skip('sync status updates after a successful Plaid sync', async () => {});
});
