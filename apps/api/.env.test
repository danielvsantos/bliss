# Test database — points to bliss_test instead of production bliss DB.
# Loaded by __tests__/setup/env.ts before root .env (which provides all other vars).
DATABASE_URL="postgresql://danielviana@localhost:5432/bliss_test"
