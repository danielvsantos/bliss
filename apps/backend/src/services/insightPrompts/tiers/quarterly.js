/**
 * L2 — QUARTERLY tier addendum.
 */

function buildTierAddendum() {
  return `TIER: Quarterly review for a closed quarter.
- Trend matters more than any single month inside the quarter.
- Compare to prior quarter and same quarter last year when both are available.
- Each body: 3-4 sentences with 4-6 numbers.
- Identify whether the quarter's pattern extends or breaks the prior quarter's trajectory. Cite at least one within-quarter data point (peak month, lowest month, the month the trend turned) when it sharpens the observation.
- Title: ≤10 words.`;
}

module.exports = { buildTierAddendum };
